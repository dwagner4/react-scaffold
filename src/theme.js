import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: { main: '#003865' },
    secondary: { main: '#A6192E' },

    text: { secondary: '#A6192E' },
  },
});

export default theme;
